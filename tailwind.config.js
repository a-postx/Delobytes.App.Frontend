/**
 * This file is no longer used with Tailwind CSS v4.
 *
 * In v4, configuration is done in CSS via @theme directive inside main.css.
 * Automatic content detection is built in — no `content` array needed.
 * The Vite plugin (@tailwindcss/vite) handles everything.
 *
 * Example of CSS-based theme customization in main.css:
 *
 * @theme {
 *   --color-brand: #4caf50;
 *   --font-sans: 'Inter', sans-serif;
 * }
 *
 * See: https://tailwindcss.com/docs/v4-beta
 */
